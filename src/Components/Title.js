function Title() {
  return (
    <section className="section--game_description">
      <h1>Tenzies</h1>
      <p>
        Roll until all dice are the same. Click each die to freeze it at its
        current value between rolls.
      </p>
    </section>
  );
}

export default Title;
